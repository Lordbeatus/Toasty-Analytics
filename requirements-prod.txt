# Production requirements - all the tools we're using

# Core dependencies
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0,<3.0.0
click>=8.1.0
websockets>=12.0  # WebSocket support
python-multipart>=0.0.6  # Form data support

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.9  # PostgreSQL adapter

# Redis (caching + Celery broker)
redis>=5.0.0
hiredis>=2.3.0  # Faster Redis parser

# Celery (background workers)
celery[redis]>=5.3.0

# Sentry (error tracking)
sentry-sdk[fastapi,sqlalchemy,celery,redis]>=1.40.0

# Monitoring
prometheus-client>=0.19.0

# Data processing
numpy>=1.24.0
scipy>=1.10.0
pandas>=2.0.0

# Development tools
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Code quality (optional)
pylint>=3.0.0
black>=23.0.0
flake8>=7.0.0

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.23.0
httpx>=0.26.0
pytest-celery>=0.0.0

# Production web server
gunicorn>=21.2.0

# Machine Learning (Neural Grader)
torch>=2.0.0
transformers>=4.30.0  # Better than uvicorn for production

# Security
cryptography>=41.0.0

# Rate limiting
slowapi>=0.1.9

# Other
requests>=2.31.0
python-multipart>=0.0.6  # For file uploads
